/*

import { useCallback } from 'react';

export function useChatbotTrigger(setIsOpen: (open: boolean) => void) {
  const triggerChatbot = useCallback(() => {
    setIsOpen(true);
  }, [setIsOpen]);

  return { triggerChatbot };
}

*/
